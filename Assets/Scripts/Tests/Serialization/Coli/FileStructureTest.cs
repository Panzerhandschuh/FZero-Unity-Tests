using System;
using System.IO;

namespace FZeroGXEditor.Tests.Serialization
{
	public class FileStructureTest
	{
		/// <summary>
		/// Serializes the specified file to a byte array and then deserializes that byte array back to an in-memory file structure
		/// </summary>
		//protected T SerializeAndDeserializeFile<T>(T file, Func<BinaryReader, T> deserializeFunc) where T : IBinarySerializable
		//{
		//	var data = file.SerializeToBytes();
		//	return IBinarySerializableExtensions.DeserializeFromBytes(data, deserializeFunc);
		//}
	}
}
